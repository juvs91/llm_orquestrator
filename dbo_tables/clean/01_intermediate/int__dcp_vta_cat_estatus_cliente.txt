with de_duplicacion_vtacatestatuscliente as (
	select
		*,
		row_number() over(
			partition by pks...
			order by
				fechaultimamod desc
		) as rownum
	from
		{{ ref('stg__vta_cat_estatus_cliente') }}
),
sel_vtacatestatuscliente as (
	select
		_airbyte_raw_id,
		_airbyte_extracted_at,
		_airbyte_meta,
		bajalogica,
		nombrepcmod,
		_ab_cdc_lsn,
		clausuariomod,
		fechaultimamod,
		_ab_cdc_cursor,
		fechabajalogica,
		claestatuscliente,
		nomestatuscliente,
		_ab_cdc_deleted_at,
		_ab_cdc_updated_at,
		nomestatusclienteing,
		_ab_cdc_event_serial_no
		from de_duplicacion_vtacatestatuscliente
	where
		rownum = 1
)
select
	*
from
	sel_vtacatestatuscliente